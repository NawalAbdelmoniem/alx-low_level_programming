#include "main.h"
#include <stdio.h>
/**
 *main - check the code
 *_puts_recursion: print string followed by new line.
 * @s : print string
 *return : nothing
*/
void _puts_recursion(char *s);
int main(void)
{
if (*s == '\0')
{
_putchar('\n');
return;
}
_putchar(*s);
s++
_puts_recursion(s);
}
