#include "main.h"

/**
 *main - check the code
 *_puts_recursion: print string followed by new line.
 * @s : print string
 */
void _puts_recursion(char *s);
int main(void)
{
int (*s)
if (*s == '\0')
{
_putchar('\n');
return (0);
}
_putchar(*s);
_puts_recursion(s + 1);
}
