#include <main.h>
#include <stdio.h>
/**
 *Write a function that swaps the values of two integers
 *int *a, int *b
 *Return: Always 0.
*/
void swap_int(int *a, int *b);
}
int c;
c = *a;
*a = *b;
*b = c;
return (0);
}
